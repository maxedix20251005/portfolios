# 引継ぎメモ（01-proposal 継続用）

## 現在の作業対象
- `docs/01-proposal.html`
- `css/style.css`

## ここまでの主な実施内容
- 章構成を `01〜07`（07はAppendix）で整理。
- 図解強化:
  - 「ファネル（簡易）」を横フロー図に変更。
  - 「2分岐での整理（視覚化）」を縦起点の分岐図に変更。
- 数式表現の統一:
  - 「構造式」ほか主要計算式を数式カード化。
  - 演算子は立体感ある表現に統一。
- 04 制作範囲:
  - `In Scope` 強調、`Out of Scope` は対象外バッジで明確化。
- 05 使用技術:
  - 技術スタック表を更新。
  - 2列目の括弧付き項目は `unverified` としてイタリック化。
  - 注釈を表直下に追加:
    - `※イタリック表記の技術は、推測情報を含む未確認項目です。`
- 06 スケジュール:
  - 「スケジュール（Phase1〜3：実日付）」の `Phase1` 行を太字化。

## 直近で確認済みのポイント
- 上記注釈の位置は `05 使用技術 > 技術スタック` 表直下に修正済み。
- 表と注釈の間隔は `proposal-v2` 内 `doc-table` の下余白を調整済み。

## 次に予定している最終ブラッシュアップ案（未着手）
1. Appendixリンクとアンカーの整合最終確認。
2. 見出し階層（`h2`/`h3`）の揺れを統一。
3. 重要表の直前に「この表の目的」1行を追加。
4. 章間接続文（02→03→04→05→06）を短文で補強。

## Windowsで再開する手順（最短）
1. Windowsで同じリポジトリを `git clone`（または既存なら `git pull`）。
2. VS Codeでこのプロジェクトを開く。
3. Codexを起動し、最初にこのファイル内容を貼って再開依頼する。

## 再開時の依頼テンプレ
以下をそのまま貼り付けて開始:

```txt
handover-notes.md を前提に、docs/01-proposal.html の最終校正を続けてください。
優先順は「リンク整合→見出し階層統一→表の目的1行追加→章間接続文追加」です。
```

## 関連ドキュメント（参照リンク）
- 作業履歴（完全版）: `work-done-by-codex.md`
- Windows再開向け10行要約: `windows-restart-10line-summary.md`
- 修正前提案（全体レビュー）: `pre-revision-proposals.md`
- サイト運用チートシート: `site-cheatsheet.md`
